{
  "eval_id": 2,
  "eval_name": "code-review",
  "prompt": "帮我检查一下这段代码有没有问题：\n```typescript\nimport TencentCloudChat from '@tencentcloud/chat';\nconst chat = TencentCloudChat.create({ SDKAppID: 1400000000 });\nasync function init(userID: string, userSig: string) {\n  await chat.login({ userID, userSig });\n  chat.on(TencentCloudChat.EVENT.KICKED_OFFLINE, () => {\n    init(userID, userSig);\n  });\n}\n```",
  "assertions": [
    {"id": "catches-listener-after-login", "text": "Flags that KICKED_OFFLINE listener is registered after login() instead of before"},
    {"id": "catches-auto-retry-in-kick", "text": "Flags the auto-retry login inside the kicked-offline handler as dangerous"},
    {"id": "explains-kick-loop-risk", "text": "Explains WHY auto-retry causes an infinite kick loop between devices"},
    {"id": "provides-fixed-code", "text": "Includes corrected code with both issues fixed"},
    {"id": "mentions-error-handling", "text": "Notes missing error code handling for UserSig expiration (6206/70001)"}
  ]
}
